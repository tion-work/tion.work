FROM node:20-alpine

WORKDIR /app

# 复制 package.json 和 package-lock.json
COPY package*.json ./

# 安装所有依赖（包括开发依赖）
RUN npm install --include=dev

# 复制源代码和配置文件
COPY src/ ./src/
COPY package.json ./
COPY tsconfig.json ./

# 暴露端口
EXPOSE 3001

# 开发模式启动命令
CMD ["npm", "run", "dev"]
